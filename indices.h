#ifndef INDICES_H
#define INDICES_H

struct Indices {
    int rho;
    int vx;
    int vy;
    int vz;
    int P;
    // Add more as needed
};

extern Indices idx; // Declare global variable

#endif // INDICES_H